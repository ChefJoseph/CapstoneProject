import React from "react";
import { Text, Image, TouchableOpacity, View, } from "react-native";
import styles from "./ButtonComponent.style";
import ArrowIMG from "../../local-assets/right-arrow.png";
const ButtonComponent = ({ buttonText = "Hotspot", onButtonPress, disableButtonIcon, buttonIconComponent, buttonContainerStyle, buttonTextStyle, buttonIconImageStyle, buttonImageSource = ArrowIMG, }) => {
    return (<TouchableOpacity style={styles.container} onPress={onButtonPress}>
      <View style={[styles.buttonContainer, buttonContainerStyle]}>
        <Text style={[styles.buttonTextStyle, buttonTextStyle]}>
          {buttonText}
        </Text>
        {!disableButtonIcon && (<View style={styles.buttonIconStyle}>
            {buttonIconComponent || (<Image resizeMode="contain" source={buttonImageSource} style={[styles.iconImageStyle, buttonIconImageStyle]}/>)}
          </View>)}
      </View>
    </TouchableOpacity>);
};
export default ButtonComponent;
//# sourceMappingURL=ButtonComponent.js.map